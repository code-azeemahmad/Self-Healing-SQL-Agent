# Self-Healing-SQL-Agent
Built a local agentic SQL system that generates, validates, executes, diagnoses, and repairs SQL queries using LangGraph and Qwen2.5-Coder. Implemented bounded stateful recovery with strict validation before database execution and streamed execution state through FastAPI SSE.
